git code:
change cd source first;
find the version and remember:
open cmd and code like:git reset --hard aa8ecad
then it will be good,but remember it can not change again
c++£º
vector:
	add vector:
		vector<int> temp

	add element
		temp.push_back(int x) add element at end